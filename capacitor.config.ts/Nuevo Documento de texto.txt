import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.presupuesto.app',
  appName: 'Presupuesto Inteligente',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;